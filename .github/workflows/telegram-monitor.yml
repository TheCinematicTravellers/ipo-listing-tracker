name: ORB Telegram Monitor

on:
  schedule:
    - cron: '*/5 4-10 * * 1-5'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Check live ORB states
        env:
          MONITOR_URL: https://our-screener.vercel.app/api/telegram-monitor
          MONITOR_SECRET: ${{ secrets.MONITOR_SECRET }}
        run: |
          curl --fail-with-body --max-time 120 \
            -H "Authorization: Bearer ${MONITOR_SECRET}" \
            "${MONITOR_URL}"

  daily-summary:
    runs-on: ubuntu-latest
    steps:
      - name: Send 4pm IST daily summary
        env:
          MONITOR_URL: https://our-screener.vercel.app/api/telegram-monitor?summary=1
          MONITOR_SECRET: ${{ secrets.MONITOR_SECRET }}
        run: |
          curl --fail-with-body --max-time 120 \
            -H "Authorization: Bearer ${MONITOR_SECRET}" \
            "${MONITOR_URL}"
