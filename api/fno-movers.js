import crypto from 'node:crypto';
import { buildLockedSet } from './orb-lock.mjs';
const IST='Asia/Kolkata';
const MASTER_URL='https://margincalculator.angelbroking.com/OpenAPI_File/files/OpenAPIScripMaster.json';
const LOGIN_URL='https://apiconnect.angelone.in/rest/auth/angelbroking/user/v1/loginByPassword';
const QUOTE_URL='https://apiconnect.angelone.in/rest/secure/angelbroking/market/v1/quote/';
const CANDLE_URL='https://apiconnect.angelone.in/rest/secure/angelbroking/historical/v1/getCandleData';
let cachedMaster=null,cachedAt=0,nextAngelRequestAt=0;
function sleep(ms){return new Promise(r=>setTimeout(r,ms))}
async function paceAngel(minGap=400){const wait=Math.max(0,nextAngelRequestAt-Date.now());if(wait)await sleep(wait);nextAngelRequestAt=Date.now()+minGap}
function b32(s){const a='ABCDEFGHIJKLMNOPQRSTUVWXYZ234567',c=s.replace(/=+$/,'').replace(/\s/g,'').toUpperCase();let bits='';for(const ch of c){const v=a.indexOf(ch);if(v<0)throw Error('Invalid TOTP secret');bits+=v.toString(2).padStart(5,'0')}const out=[];for(let i=0;i+8<=bits.length;i+=8)out.push(parseInt(bits.slice(i,i+8),2));return Buffer.from(out)}
function totp(secret,now=Date.now()){const b=Buffer.alloc(8);b.writeBigUInt64BE(BigInt(Math.floor(now/1000/30)));const h=crypto.createHmac('sha1',b32(secret)).update(b).digest(),o=h[h.length-1]&15;const n=((h[o]&127)<<24)|((h[o+1]&255)<<16)|((h[o+2]&255)<<8)|(h[o+3]&255);return String(n%1000000).padStart(6,'0')}
function headers(apiKey,jwt,mac){return {'Content-Type':'application/json','Accept':'application/json','X-UserType':'USER','X-SourceID':'WEB','X-ClientLocalIP':'127.0.0.1','X-ClientPublicIP':process.env.ANGEL_CLIENT_PUBLIC_IP||'127.0.0.1','X-MACAddress':mac,'X-PrivateKey':apiKey,...jwt?{Authorization:`Bearer ${jwt}`}:{}}}
function norm(s){return String(s).trim().toUpperCase().replace(/-EQ$/,'')}
async function redisCommand(command){const base=process.env.UPSTASH_REDIS_REST_URL,token=process.env.UPSTASH_REDIS_REST_TOKEN;if(!base||!token)throw Error('Missing Upstash Redis environment variables');const r=await fetch(base,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(command)}),d=await r.json();if(!r.ok||d.error)throw Error(d.error||`Redis HTTP ${r.status}`);return d.result}
const redisGet=key=>redisCommand(['GET',key]);
const redisSet=(key,value,ttl=172800,nx=false)=>redisCommand(n?['SET',key,value,'EX',String(ttl),'NX']:['SET',key,value,'EX',String(ttl)]);
async function master(){if(cachedMaster&&Date.now()-cachedAt<600000)return cachedMaster;const r=await fetch(MASTER_URL,{headers:{Accept:'application/json'},cache:'no-store'});if(!r.ok)throw Error(`Instrument master HTTP ${r.status}`);const map=new Map();for(const x of await r.json()){if(x.exch_seg!=='NSE'||x.instrumenttype)continue;for(const k of [x.symbol,x.tradingsymbol]){const n=norm(k||'');if(n&&!map.has(n))map.set(n,x.token)}}cachedMaster=map;cachedAt=Date.now();return map}
async function login(){const apiKey=process.env.ANGEL_API_KEY,client=process.env.ANGEL_CLIENT_ID,pin=process.env.ANGEL_PIN,secret=process.env.ANGEL_TOTP_SECRET,mac=process.env.ANGEL_MAC_ADDRESS||'00:00:00:00:00:00';if(!apiKey||!client||!pin||!secret)throw Error('Missing Angel One environment variables');await paceAngel();const r=await fetch(LOGIN_URL,{method:'POST',headers:headers(apiKey,null,mac),body:JSON.stringify({clientcode:client,password:pin,totp:totp(secret)})}),d=await r.json();if(!r.ok||!d?.data?.jwtToken)throw Error(`Angel login failed: ${d?.message||r.status}`);return {apiKey,jwt:d.data.jwtToken,mac}}
async function quote(a,tokens){await paceAngel();const r=await fetch(QUOTE_URL,{method:'POST',headers:headers(a.apiKey,a.jwt,a.mac),body:JSON.stringify({mode:'FULL',exchangeTokens:{NSE:tokens}})}),d=await r.json();if(!r.ok||d?.status===false)throw Error(`Quote failed: ${d?.message||r.status}`);return d?.data?.fetched||[]}
async function angelCandleRequest(a,token,from,to,interval){let lastError=null;for(let attempt=0;attempt<4;attempt++){await paceAngel();try{const r=await fetch(CANDLE_URL,{method:'POST',headers:headers(a.apiKey,a.jwt,a.mac),body:JSON.stringify({exchange:'NSE',symboltoken:String(token),interval,fromdate:from,todate:to})}),d=await r.json();if(r.ok&&d?.status!==false)return d?.data||[];lastError=Error(`Candle failed: ${d?.message||r.status}`);const msg=String(d?.message||'').toLowerCase();if(r.status!==429&&!msg.includes('rate')&&!msg.includes('throttle')&&!msg.includes('too many'))break}catch(e){lastError=e}await sleep(800*(attempt+1))}throw lastError||Error('Candle request failed')}
const num=x=>{const n=Number(x);return Number.isFinite(n)?n:null};
function parts(d=new Date()){const p=new Intl.DateTimeFormat('en-CA',{timeZone:IST,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(d),o={};for(const x of p)o[x.type]=x.value;return o}
function stamp(){return new Intl.DateTimeFormat('en-IN',{timeZone:IST,day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:true}).format(new Date())}
function orderedCandles(arr){return (Array.isArray(arr)?arr:[]).filter(c=>Array.isArray(c)&&c.length>=5&&Number.isFinite(Date.parse(String(c[0])))).slice().sort((a,b)=>Date.parse(String(a[0]))-Date.parse(String(b[0])))}
function buildOrb5m(oneMin){const groups=new Map();for(const c of orderedCandles(oneMin)){const p=parts(new Date(String(c[0]))),m=Number(p.minute),h=Number(p.hour);if(h!==9||m<15||m>=30)continue;const bucket=15+Math.floor((m-15)/5)*5,key=`${p.year}-${p.month}-${p.day} ${String(h).padStart(2,'0')}:${String(bucket).padStart(2,'0')}`,o=num(c[1]),hi=num(c[2]),lo=num(c[3]),cl=num(c[4]);if([o,hi,lo,cl].some(v=>v===null))continue;const g=groups.get(key);if(!g)groups.set(key,[c[0],o,hi,lo,cl]);else{g[2]=Math.max(g[2],hi);g[3]=Math.min(g[3],lo);g[4]=cl}}return [...groups.values()].sort((a,b)=>Date.parse(String(a[0]))-Date.parse(String(b[0])))}
export function stateFor(orbCandles,oneMin,direction){const orb=orderedCandles(orbCandles).slice(0,3);if(orb.length<3)return {status:'⏳ Pending',result:'⏳'};const hi=Math.max(...orb.map(c=>Number(c[2]))),lo=Math.min(...orb.map(c=>Number(c[3])));if(!Number.isFinite(hi)||!Number.isFinite(lo)||hi<=lo)return {status:'⏳ Pending',result:'⏳'};const entry=direction==='LONG'?hi:lo,sl=direction==='LONG'?lo:hi,risk=Math.abs(entry-sl),target=direction==='LONG'?entry+0.4*risk:entry-0.4*risk;let active=false;for(const c of orderedCandles(oneMin)){const h=Number(c[2]),l=Number(c[3]);if(!Number.isFinite(h)||!Number.isFinite(l))continue;if(!active){if(direction==='LONG'){const e=h>=entry,s=l<=sl;if(e&&s)return {status:'❌ SL',result:'❌'};if(e){active=true;if(h>=target)return {status:'🎯 Target',result:'0.4R ✅'}}else if(s)return {status:'⚠️ Invalidated level',result:'⚠️'}}else{const e=l<=entry,s=h>=sl;if(e&&s)return {status:'❌ SL',result:'❌'};if(e){active=true;if(l<=target)return {status:'🎯 Target',result:'0.4R ✅'}}else if(s)return {status:'⚠️ Invalidated level',result:'⚠️'}}}else if(direction==='LONG'){const t=h>=target,s=l<=sl;if(t&&s)return {status:'❌ SL',result:'❌'};if(t)return {status:'🎯 Target',result:'0.4R ✅'};if(s)return {status:'❌ SL',result:'❌'}}else{const t=l<=target,s=h>=sl;if(t&&s)return {status:'❌ SL',result:'❌'};if(t)return {status:'🎯 Target',result:'0.4R ✅'};if(s)return {status:'❌ SL',result:'❌'}}}return active?{status:'✅ Trade Active',result:'⚖️'}:{status:'⏳ Pending',result:'⏳'}}
function rowFromQuote(x,q){const open=num(q?.open),high=num(q?.high),low=num(q?.low),cmp=num(q?.ltp),close=num(q?.close),volume=num(q?.tradeVolume);if([open,high,low,cmp,close].some(v=>v===null)||!close)return null;return {...x,open,high,low,cmp,volume,change_pct:Number(((cmp/close-1)*100).toFixed(2))}}
const lockedKey=date=>`orb:locked:${date}`;
async function readLocked(date){const raw=await redisGet(lockedKey(date));return raw?JSON.parse(raw):null}
async function lockUniverse(date,gainers,losers,lockedAt){const candidate=buildLockedSet(gainers,losers).map(x=>({...x,locked_at_ist:lockedAt,locked_change_pct:x.change_pct}));const result=await redisCommand(['SET',lockedKey(date),JSON.stringify(candidate),'EX','172800','NX']);if(result===null){const existing=await readLocked(date);if(existing)return existing;throw Error('Unable to create or read locked ORB universe')}return candidate}
async function statusForLocked(a,row,date,start,end){const key=`orb:status:${date}:${row.symbol}`,cached=await redisGet(key);if(cached){const parsed=JSON.parse(cached);if(['🎯 Target','❌ SL','⚠️ Invalidated level'].includes(parsed.status))return parsed}const combined=orderedCandles(await angelCandleRequest(a,row.token,start,end,'ONE_MINUTE')),orb=buildOrb5m(combined);if(orb.length<3)throw Error(`Insufficient ORB 5m data: ${orb.length}`);const postOrb=combined.filter(c=>Date.parse(String(c[0]))>=Date.parse(`${date.slice(0,4)}-${date.slice(4,6)}-${date.slice(6,8)}T09:30:00+05:30`)),computed=stateFor(orb,postOrb,row.direction),previous=cached?JSON.parse(cached):null,final=previous?.status==='✅ Trade Active'&&computed.status==='⏳ Pending'?previous:computed;await redisCommand(['SET',key,JSON.stringify(final),'EX','172800']);return final}
export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','GET,OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');res.setHeader('Cache-Control','no-store,max-age=0');if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return res.status(405).json({error:'GET only'});try{const p=parts(),mins=Number(p.hour)*60+Number(p.minute),date=p.year+p.month+p.day,start=`${p.year}-${p.month}-${p.day} 09:15`,end=`${p.year}-${p.month}-${p.day} ${p.hour}:${p.minute}`,raw=await fetch('https://raw.githubusercontent.com/TheCinematicTravellers/ipo-listing-tracker/main/fno_universe.json',{cache:'no-store'}),symbolsData=await raw.json(),symbols=Array.isArray(symbolsData)?symbolsData:JSON.parse(symbolsData.content),m=await master(),a=await login(),locked=await readLocked(date);if(mins<570){const matched=symbols.map(s=>({symbol:s,token:m.get(norm(s))})).filter(x=>x.token),rows=[];for(let i=0;i<matched.length;i+=50){const batch=matched.slice(i,i+50),f=await quote(a,batch.map(x=>String(x.token))),by=new Map(f.map(x=>[String(x.symbolToken),x]));for(const x of batch){const r=rowFromQuote(x,by.get(String(x.token)));if(r)rows.push(r)}}const gainers=rows.filter(x=>Math.abs(x.open-x.low)<1e-7).sort((a,b)=>b.change_pct-a.change_pct).slice(0,10),losers=rows.filter(x=>Math.abs(x.open-x.high)<1e-7).sort((a,b)=>a.change_pct-b.change_pct).slice(0,10);return res.status(200).json({date_ist:new Intl.DateTimeFormat('en-IN',{timeZone:IST,day:'2-digit',month:'short',year:'numeric'}).format(new Date()),updated_ist:stamp(),timezone:IST,universe_size:symbols.length,available:rows.length,locked:false,gainers,losers})}let lockedSet=locked;if(!lockedSet){const matched=symbols.map(s=>({symbol:s,token:m.get(norm(s))})).filter(x=>x.token),rows=[];for(let i=0;i<matched.length;i+=50){const batch=matched.slice(i,i+50),f=await quote(a,batch.map(x=>String(x.token))),by=new Map(f.map(x=>[String(x.symbolToken),x]));for(const x of batch){const r=rowFromQuote(x,by.get(String(x.token)));if(r)rows.push(r)}}const gainers=rows.filter(x=>Math.abs(x.open-x.low)<1e-7).sort((a,b)=>b.change_pct-a.change_pct).slice(0,10),losers=rows.filter(x=>Math.abs(x.open-x.high)<1e-7).sort((a,b)=>a.change_pct-b.change_pct).slice(0,10);lockedSet=await lockUniverse(date,gainers,losers,`${p.hour}:${p.minute} IST`)}const tokens=lockedSet.map(x=>String(m.get(norm(x.symbol)))).filter(Boolean),f=await quote(a,tokens),by=new Map(f.map(x=>[String(x.symbolToken),x])),out=[],statusErrors=[];for(const x of lockedSet){const token=m.get(norm(x.symbol)),r=rowFromQuote({...x,token},by.get(String(token)));if(!r)continue;try{const s=await statusForLocked(a,r,date,start,end);r.status=s.status;r.result=s.result}catch(e){statusErrors.push({symbol:r.symbol,error:e.message||String(e)});r.status='⏳ Pending';r.result='⏳'}out.push(r)}const gainers=out.filter(x=>x.direction==='LONG'),losers=out.filter(x=>x.direction==='SHORT');return res.status(200).json({date_ist:new Intl.DateTimeFormat('en-IN',{timeZone:IST,day:'2-digit',month:'short',year:'numeric'}).format(new Date()),updated_ist:stamp(),timezone:IST,universe_size:symbols.length,available:out.length,matched:out.length,missing:[],locked:true,locked_at_ist:lockedSet[0]?.locked_at_ist||null,gainers,losers,status_errors:statusErrors})}catch(e){console.error(e);return res.status(500).json({error:e.message||'AUTO feed failed'})}}
